enclave {

    trusted {
        public void ecall_validate_empty_disk([in, string] const char* path);  
        public void ecall_validate_meaningful_disk([in, string] const char *hashs, [in, size=sizes_len] const size_t *sizes, size_t sizes_len);        
    };
    
};