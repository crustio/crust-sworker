#ifndef _CRUST_IPFS_H_
#define _CRUST_IPFS_H_

#include <stdio.h>
#include <string>
#include <vector>

class Ipfs
{
private:
    /* data */
public:
    Ipfs();
    ~Ipfs();
};

#endif /* !_CRUST_APP_H_ */
